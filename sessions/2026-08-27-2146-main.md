---
session: 1
slug: main
branch: main
started: 2026-08-27T21:46:23Z
ended: 2026-08-29T01:57:33Z
points: 14
pr_numbers: [5, 6, 7, 8, 9]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-jig/e2e0f207-e556-5141-928a-80928cc24e77.jsonl
---

# Session 1 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Make the schema-v1 gate adoptable by repos with existing records

**Completed:**
- `scripts/check-decisions.mjs` — `legacyVerdict` replaces a branch hardcoded to jig's own empty-corpus history. Shared `RECORD_DIRS`, `frontmatterBlock`, `recordBody`; `beforeAmendments` deleted and `sizeOf` measures whole files
- `scripts/gen-decisions-baseline.mjs` — new; run once at adoption, writes id + fingerprint per legacy record
- `scripts/check-decisions.test.mjs` — 12 new cases, all written before the code and observed red
- `CLAUDE.md` — amendment convention replaced with supersession plus the freeze rule
- `docs/decisions/` — DEC-J001 split (it was 3,835 bytes holding a second decision inside an amendment); DEC-J002, J003, J004, J005 added
- Also this session, not in this PR: soundings brought to jig v6 on `task/v6-migration` (35 files, 4 of 5 gates green before this fix, all 5 after)

**Code review:** `@code-review` found 2 real bugs, both fixed — the generator never baselined `docs/decisions/archive/`, and the two scripts extracted frontmatter differently so a CRLF or BOM record failed forever as `not-listed`. Both failed in the same direction: an untouched legacy record rejected, which is the opposite of the class this gate closes. `/security-review` found nothing at threshold; its two non-security observations were real and fixed.

**PR:** [PR #5](https://github.com/mobiustripper42/jig/pull/5)
**Points:** 5
**Branch:** task/schema-gate-adoptable
**Opened at:** 2026-08-28T01:34:00Z

## Task 2: Ship the decision schema to the projects whose gate reads it

**Completed:**
- `.claude/file-classes.yaml` — `docs/decisions/decision-record.schema.json` carved out as `logic`, above the `docs/**: jig-only` glob
- `scripts/drift.mjs` — `EXCLUSION_EXCEPTIONS`; the `docs/decisions/` prefix skip ran before classification and made the registry line unreachable
- `scripts/drift.test.mjs` — one case, red first, then tightened after review

**Code review:** 2 findings, both cleanup. The test matched a bare path that `UNCLASSIFIED` also prints, so half the fix went green — anchored to the bucket and mutation-checked both ways. The second finding is scope, disclosed in the PR: drift only reports, so soundings still lacks the file after this merges.

**PR:** [PR #6](https://github.com/mobiustripper42/jig/pull/6)
**Points:** 2
**Branch:** task/ship-the-decision-schema
**Opened at:** 2026-08-28T04:52:00Z

## Task 3: Delete two Workflow Notes that outlived what they described

**Completed:**
- `.claude/CLAUDE-context.md` — cut "jig develops straight to `main`" (true for the seeds port only; it cost today's revert) and "red is expected from muster-coupled tests" (task 3.3 fixed those; 203 pass). Replaced with nothing — jig follows the shell now, and that is not a fact worth a line
- `docs/PROJECT_PLAN.md`, `docs/ANALYSIS.md` — the same claim's other two copies, found by review

**Code review:** 3 findings. Two fixed: the claim had three copies and the first commit removed one — PROJECT_PLAN still said a phase "closes without PRs to merge" while this repo has merged PRs #1, #5 and #6. The third is open and needs a decision: `kill-this/SKILL.md:53` still pushes straight to `main` on an unprotected main, and `origin/main` is genuinely unprotected. The stated permission is gone; the mechanism is not.

**PR:** [PR #7](https://github.com/mobiustripper42/jig/pull/7)
**Points:** 2
**Branch:** task/retire-the-straight-to-main-line
**Opened at:** 2026-08-28T12:10:00Z

## Task 4: Stop the shared schema from talking about one repo

**Completed:**
- `docs/decisions/decision-record.schema.json` — both `$comment` fields rewritten. They named jig and muster in a `logic` file that installs byte-identical everywhere, and had already shipped into soundings via its PR #86
- Both were stale too: they described the unconditional skip DEC-J004 replaced with the baseline

**Code review:** 3 findings. Fixed: `revisit_if`'s comment claimed "nothing else in the schema retires a record", contradicting the `status` enum three properties up — reworded to mark-vs-cause. Two left for a follow-up, same defect class in other `logic` files: `docs/AGENTS.md` ships jig's audit history (dated status line, invocation counts, cost estimate, word counts) and `docs/VELOCITY_AND_POKER_GUIDE.md:33-39` hardcodes `~/bushel ~/muster ~/helm`.

**PR:** [PR #8](https://github.com/mobiustripper42/jig/pull/8)
**Points:** 2
**Branch:** task/schema-comments-that-arent-about-jig
**Opened at:** 2026-08-28T12:45:00Z

## Task 5: Stop two shipped docs from being about jig, and rescue the tool they cite

**Completed:**
- `docs/AGENTS.md` — title, framing, and a dated status block about jig's own Phase 2 progress, all shipped byte-identical to every project. Roster and retirement measurements kept: those are historical facts about a decision, not current state
- `docs/VELOCITY_AND_POKER_GUIDE.md` — dropped `~/bushel ~/muster ~/helm` and the pointer into the archived seeds checkout
- `scripts/throughput.py` + `docs/THROUGHPUT_QUICKREF.md` — brought over from seeds. `scripts/**: jig-only` already classified the script, so no registry change
- `.gitignore` — `__pycache__/`, from my own `py_compile`

**Code review:** 3 findings, all real, all in the quickref I copied without reading — the same mistake as PR #8's schema, one file over. It still said "from your seeds checkout" with the old paths; it claimed the methodology guide's Part 1 was stale hrs/pt when that guide describes throughput in its second paragraph; and it pointed at `docs/DECISIONS.md` for DEC-S026, which starts at DEC-J001. No gate sees any of the three — all are prose about state, and verify was green before and after.

**PR:** [PR #9](https://github.com/mobiustripper42/jig/pull/9)
**Points:** 3
**Branch:** task/shipped-docs-that-arent-about-jig
**Opened at:** 2026-08-28T13:40:00Z

**Next Steps:**
- **Muster migration, two tasks.** A: the v6 sync — 15 files, 2 scripts added, 7 retirements, `jig-version 6`, `check:denied` wired, `gen-decisions-baseline` to freeze 150 legacy records. B: the 283-line context file audit. Muster already has the dictionary machinery, so that half does not repeat
- **Muster is otherwise ready:** PR #855 gave all 9 `schema: 1` records a `revisit_if`, all clear `minLength: 10`; no `archive/`
- **Enable branch protection on jig's `origin/main`** — open finding from PR #7
- After PR #8: sync the schema into soundings (two `$comment` lines), then start muster
- **Muster is ready otherwise:** all 9 of its `schema: 1` records now carry `revisit_if` and clear `minLength: 10`; the other 150 baseline and freeze; no `archive/`
- **Enable branch protection on jig's `origin/main`** — open finding from PR #7. Without it `/kill-this` can still push unreviewed
- Soundings PR #86 installs `decision-record.schema.json`, unblocking DEC-013
- Soundings PR #84 (`one-piece` output style) is open and unmerged
- Ship output styles from jig as a `logic` class so every repo carries them and `settings.local.json` picks — discussed, not built
- `reports` is back in the not-affected list in the v2 style file; the word we cut
- `issue #4` — `decision-record.schema.json` is classed `jig-only` but every project's gate reads it. Silent until the first legacy conversion anywhere
- Muster is still at seeds-version 5 and hits the same wall soundings did (Phase 4)
- A custom output style: `Concise` gets brevity right but says nothing about one topic per turn. Parked deliberately

**Context:**
- **jig no longer develops straight to `main`.** The line saying so in `.claude/CLAUDE-context.md` is stale — it described the seeds port only. It has not been removed yet, and it caused a real failure this session: work was committed and pushed to `main` with no review, human or agent. Recovered by branching the commit, reverting `main` (95385f9), and re-landing through this PR
- `/its-alive` Step 0.6 contradicted the shell's "output pushes for the user to run." DEC-J002 resolves it: invoking a skill approves the pushes in its own ritual. Muster had the same fix scoped to `/kill-this` alone, in a project file
- The `sessions` branch was bootstrapped this session via plumbing rather than the skill's `git checkout --orphan` + `git rm -rf .`, which would have emptied the working tree

**Context:**
