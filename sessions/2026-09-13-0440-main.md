---
session: 4
slug: main
branch: main
started: 2026-09-13T04:40:48Z
ended: 2026-09-17T21:32:52Z
points: 0
pr_numbers: []
status: closed
transcript: /home/eric/.claude/projects/-home-eric-jig/ef8c2333-551c-54fa-afbb-6b03cba84677.jsonl
---

# Session 4 — main

<!-- Task blocks appended by /kill-this, one per task. -->

**Next Steps:**

- **Four issues were filed and none worked.** jig [issue #35](https://github.com/mobiustripper42/jig/issues/35) — a tape filename says which session, not which repo; the fix is `/its-dead` Step 4.8 prefixing the repo folder. jig [issue #36](https://github.com/mobiustripper42/jig/issues/36) — the Fable 5.1 changes to `one-piece.md`, blocked on deciding which copy is the draft. tinkle [issue #182](https://github.com/mobiustripper42/tinkle/issues/182) — migrate to jig v6, 10 DRIFT files, ~5 points. tinkle [issue #183](https://github.com/mobiustripper42/tinkle/issues/183) — adopt the decision-record toolchain or decide not to.
- **jig #36 is blocked on a harvest call, not on the edits.** Muster's `one-piece.md` is ahead of jig's on purpose — it carries a tagged reply-kind system (commit `d79f35a`, "One piece v3") that jig's copy lacks. Today the consumer holds the working draft and jig holds the stale canonical. Precedent for fixing that is PR #12, which harvested one-piece v2 back from soundings.
- **Tinkle work belongs in a tinkle session.** `/kill-this` runs bare `git` against the session's own checkout (`kill-this/SKILL.md:41`, `:53`), so edits made to another repo from a jig session have no shipping path.
- **The muster/soundings/sheepdog sync counts are still Session 2's**, unmeasured since. Re-run `node scripts/drift.mjs ../<repo>` before acting on any of them.
- **Twenty-odd merged task branches** are still local and on origin. Reaping them is an explicit ask, never a session-close chore.

**Context:**

- **Disabling auto memory is a documented setting, not a permission deny.** `autoMemoryEnabled: false` in `~/.claude/settings.json`, per https://code.claude.com/docs/en/memory. There is also `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1`. This session first built the wrong thing — two `Write`/`Edit` denies on the memory glob, committed to the master and propagated — on an unverified assumption about how the feature writes its files. `@code-review` caught it and pointed at the evidence sitting in the session's own context: `MEMORY.md` arrives as an injected system-reminder, never a `Read` call, so no permission rule could ever have gated it. The whole branch was abandoned. **`CLAUDE.md` already bans this** — "Don't guess a third-party service's shapes… Never write code against a guess" — and the guess was made anyway, four times over, before anyone opened the docs.
- **The policy script compares `permissions` by strict set-equality**, so an entry present only in `~/.claude/settings.json` reports `STALE` forever and the printed fix command silently deletes it. Keys outside `MACHINE_KEYS` (`settings-policy.mjs:216`) are left alone, which is why `autoMemoryEnabled` sits there safely and a deny would not have.
- **jig now runs `One piece`** via `.claude/settings.local.json`, gitignored, effective next session. Muster and soundings already did; sheepdog and tinkle still fall through to `Concise` from the machine file.
- **Two names for one file caused most of the confusion in this session.** `~/.claude/settings.json` was called both "user settings" and "machine settings" across consecutive turns, and the operator reasonably asked whether two different files had to match. `one-piece.md` has the rule that would have prevented it, under "One name per thing"; `Concise`, which was active, does not.
- **The `Concise` carve-out "Never trade correctness for brevity" is a self-judged exemption** and fires whenever the model decides something is correctness-bearing, which was every long answer here. Worth removing if a custom style is ever written.
- **Wall clock on this session is meaningless** — it spans four calendar days across long gaps. `/retro`'s break inference is the only number worth reading.
