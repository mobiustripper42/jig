---
session: 7
slug: main
branch: main
started: 2026-09-24T00:54:27Z
ended:
points:
pr_numbers: [47, 48, 49, 50]
status: open
transcript: /home/eric/.claude/projects/-home-eric-jig/ad419fa8-d211-5d5c-abc4-8ed7f2968928.jsonl
---

# Session 7 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: drift refuses a false all-clear

**Completed:**

- **`scripts/drift.mjs`** — the `nothing differs.` guard now reads all five finding variables, not three. `notRun` and `notYours` were missing from it, and both print *below* the summary line, so a project whose only problems were an unwired gate or a jig-only copy got the all-clear as its headline with the finding underneath. That is the arrangement that let `check-denied` sit switched off in muster for three days while every drift run read clean.
- **Nothing replaces the line when it is withheld**, at the operator's call. The `NOT RUN` and `NOT YOURS` blocks already carry their own footers, and a second summary line is one more sentence to keep true.
- **`scripts/drift.test.mjs`** — three cases under `describe('the all-clear line')`, built on a **minimal fake jig** rather than the real one. That is the part worth remembering: against the real jig a throwaway project is absent two dozen templates, so `rows` is never empty, the all-clear is unreachable, and a test written the usual way would have passed against the broken code while asserting nothing. Holding `jig-version` is the whole test for a jig checkout (`drift.mjs:87`), so the fixture is three files.
- **Watched red first**, both withholding cases, and each asserts the finding block is present as well as the all-clear being absent — asserting only the absence would go green if the fixture quietly stopped producing a finding.
- The first of the three, the clean control, passes under both old and new code by design: it proves the fixture can produce a genuine all-clear, which is what makes the other two failing mean something.
- Full gate green: 296 tests across 10 files, all five `check:*` clean.
- This is the third of session 6's three mechanism gaps, and the one it called strongest — it turns step three of the sync checklist from remembered into enforced.

**Code review:** 0 findings. The reviewer did not take the red→green claim on trust: it reverted the guard in a scratch copy, confirmed both tests fail for the stated reason rather than a fixture error, and confirmed green on restore. It also independently verified the fake-jig necessity claim, that the fixture fails loud rather than silently staling, and that six print sites map onto exactly the five guard variables with no sixth finding path. `/security-review` not run — no blast-radius trigger; the diff is `drift.mjs` and its test, and the table covers `.claude/agents/**`, `.claude/skills/**`, `CLAUDE.md`, `.claude/settings.json` and `settings-policy.mjs`.

**PR:** [PR #47](https://github.com/mobiustripper42/jig/pull/47)
**Points:** 2
**Branch:** task/drift-refuses-a-false-all-clear
**Opened at:** 2026-09-24T02:21:56Z

## Task 2: Nothing ships a bare citation of a path jig keeps

**Completed:**

- **`scripts/check-shipped-citations.mjs`** — a jig-only gate refusing to let a file a project receives cite a path jig keeps to itself. The fix for a finding is the `<jig>/` prefix the skills already use; `isClaim` declines anything containing `<` (`check-context.mjs:84`), so it costs nothing to honour.
- **It cannot be a shipped gate and cannot be caught downstream in time.** Every citation in jig resolves in jig, so jig's own gates are green by construction; the defect exists only in the copy, after the sync. `check:context` downstream is not broken — it correctly reports a path that is not there, days later, looking like the receiving project's problem.
- **Jig-only despite a `check-*` name**, via a carve-out above `scripts/check-*.mjs: logic` in `.claude/file-classes.yaml`. Shipped, every project would hold a gate it cannot run and `drift.mjs` would report it NOT RUN forever. Not in `docs/CHEATSHEET.md` for the same reason: that doc ships, and `check:docs` asserts every npm script a doc names resolves.
- **`scripts/lib/file-classes.mjs`** — the registry reader extracted from `drift.mjs` and shared. `drift.mjs` cannot be imported; it runs its comparison at module scope. Its 32 tests passed unchanged, which is the extraction's proof.
- **Three subtractions are most of the work.** Run naively the gate finds 17 on jig's own corpus and all 17 are noise: `scripts/**` is out of scope (a path in a code comment is prose, and no gate resolves one); jig's own file at a scaffold target is not a template (the DEC-S049 collision from the subject side, which I had only guarded on the target side); and a path jig does not hold is not a path jig keeps (the registry classifies by glob, so `docs/**: jig-only` answered for a muster document this repo has never had).
- **Watched red properly**: wrote a stub reporting clean, watched the planted-defect test fail against it, then implemented. Then mutation-checked every guard — which caught a test that passed against *any* implementation because its fixture had no `docs/` directory, so `isClaim` rejected the citation before the class was consulted.
- **It fixes zero files.** PR #41 repaired the only bare citation that ever shipped, and I confirmed both muster and centerline carry that fix on their `origin/main`. This is preventive, and the operator decided it was worth building after the estimate said so plainly.
- Full gate green: 306 tests across 11 files.

**Code review:** 3 findings, all real, all fixed in a second commit. The bug: `check()` called `fileClasses('.')` bare, so a missing registry or a non-root cwd answered with a raw Node stack trace — the new lib's own module note says a caller owns its message and this one never wrote it; now exit 2 with a sentence, matching `drift.mjs`. The vacuous test: the decisions-exemption fixture cited the bare directory `docs/decisions/`, which `templates()` never yields, so an earlier guard absorbed it and the branch was never reached — the reviewer deleted the branch and watched all nine stay green. The wrong number: "four docs collide" is six, counted rather than copied. `/security-review` not run — no blast-radius trigger, though `.claude/file-classes.yaml` decides what ships and is arguably a gap in that table.

**Also caught by running my own test plan:** the published planting command asserted 3 matches and there is 1 — the other two `keep-tape` citations are a `.test.mjs` and a `:56` line reference. The assertion refused and planted nothing, which is the rule about scripted edits working as intended on a command I had already published. Corrected, then re-ran the whole plan end to end and confirmed the output matches what the PR body claims.

**PR:** [PR #48](https://github.com/mobiustripper42/jig/pull/48)
**Points:** 3
**Branch:** task/nothing-ships-a-bare-jig-only-citation
**Opened at:** 2026-09-24T16:25:36Z

## Task 3: The output-style symlink is checked, not remembered

**Completed:**

- **`styleProblems` in `scripts/settings-policy.mjs`** — checks that `~/.claude/output-styles/<style>.md` is a symlink into this checkout. Sits beside `hookProblems`, which makes the same argument about `keep-tape`: installed by hand is not allowed to also mean remembered.
- **Fires only when `outputStyle` names a style jig ships**, matched on the style's frontmatter `name` rather than a slug of its filename. Reported, never repaired.
- **Seven findings, because the fixes differ:** absent, a regular file, a link into another checkout, a link whose target is gone, an unreadable directory, a style file jig cannot read a name out of, and two styles claiming one name.
- Renamed `check()`'s `style` flag to `machine`; corrected two `CLAUDE.md` sentences saying nothing checks the symlink.
- Full gate green: 318 tests across 11 files.

**Code review:** 3 from `@code-review`, 0 at the bar from `/security-review` with 2 sub-threshold fixed anyway. The one that mattered: a style file with a dropped `name:` key made the whole check go dark on every machine while reporting all clear — the exact self-silencing it was written to end. Also duplicate names picking the first by directory order, and `EACCES` reported as "link was never created" with the `ln -sfn` that cannot fix it. From security: the pasteable fix line now refuses a filename that is not plain, matching `keep-tape.mjs`; `readlinkSync` inside the `realpathSync` catch is wrapped.

**Caught myself, before review:** one of my own tests was vacuous — the frontmatter-name test expected `[]` from both spellings, which a slug implementation also returns by failing to find the file at all. Two ways of producing an empty array are indistinguishable. Rewritten to assert a finding.

**Process note for the retro:** this was a 2 and shipped as one, but the operator's reading cost was wrong — spec, review write-up and security report were far longer than the change warranted, and the PR arrived late behind them. The estimate sizes the work, not the words. Worth a look at whether `/kill-this` and the review write-ups need a length discipline proportional to points.

**PR:** [PR #49](https://github.com/mobiustripper42/jig/pull/49)
**Points:** 2
**Branch:** task/the-symlink-is-checked-not-remembered
**Opened at:** 2026-09-24T18:03:56Z

## Task 4: The surface-check step is retired

**Completed:**

- **Removed the micro workflow's surface-check step and its `Surface check` slot** from `CLAUDE.md`, `.claude/CLAUDE-context.md` and `scaffold/claude/CLAUDE-context.md`. Nineteen words carrying two instructions with no stateable outcome — the prove-it step says *watch it fail for the reason you expect*, this said nothing about what to produce.
- **Read three ways in this session alone**, which is what made the case: a fixture built and read (PR #47), a planting command written into a pull request body for the reviewer (PR #48), the function called directly (PR #49). Each felt like compliance. It was also the only step leaving no evidence behind.
- **Both halves had homes.** Showing a person a new surface folds into the stop, where the waiting already happens; earlier means human review before `@code-review`, the same work twice. The tape-queue 6-vs-4 lesson survives in jig's context file as *assert the output string* — a check rather than a reminder.
- **The sweep found 8 occurrences across 4 files**, two of which reading alone would have missed: `docs/CHEATSHEET.md` cited "step 8 is a hard stop", a number the renumbering makes wrong in a `logic` doc that ships everywhere; and `CLAUDE.md` cited "step 4" three lines above its own ban on step-number citations.
- **`DEC-J008`** records it. Search returned nothing; new id, nothing superseded. Four passes to fit under the 2,000-byte cap.
- Full gate green: 318 tests across 11 files.

**Code review:** 2 findings from `@code-review`, both fixed. It found three *more* step-number citations in `CLAUDE.md` (lines 25, 103, 165) after my commit message claimed every one was gone — correct today only because steps 1 and 4 did not move, which is exactly the staleness the branch exists to close. Also that `DEC-J008`'s claims omitted `.claude/CLAUDE-context.md`, the one changed file carrying the replacement lesson. `/security-review` ran (`CLAUDE.md` is a blast-radius trigger) and found no weakened control: the `/kill-this` requirement survived byte-for-byte.

**Worth noting for the retro:** `/security-review` on a prose deletion cost a full agent run to confirm nothing. The blast-radius table fires on `CLAUDE.md` by path, with no notion of whether the diff contains code. Same family as the "review write-ups scale with points" item.

**PR:** [PR #50](https://github.com/mobiustripper42/jig/pull/50)
**Points:** 2
**Branch:** task/step-7-goes
**Opened at:** 2026-09-24T19:10:32Z

**Next Steps:**

**Context:**
