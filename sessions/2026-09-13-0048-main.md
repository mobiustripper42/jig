---
session: 3
slug: main
branch: main
started: 2026-09-13T00:48:11Z
ended: 2026-09-13T04:36:44Z
points: 2
pr_numbers: [34]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-jig/b394aba4-d805-508a-96ff-31d67cdda7b4.jsonl
---

# Session 3 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Issue #32 — run test files one at a time, so the archive tests stop racing the corpus readers

**Completed:**
- `package.json` — `"test": "vitest run --no-file-parallelism"`. One line. Serial adds about 1.4s (2.26s → 3.64s).
- `.claude/CLAUDE-context.md` § Workflow Notes — why, citing issue #32, and why `describe.sequential` would not have done it: it orders tests within a file, and the collision is across files.
- Both archive tests untouched. The pair has to be real (`DEC-J005` cites `DEC-J004`), so the tests stay and the parallelism goes.

**Not built:** a `vitest.config.mjs` with projects and `groupOrder` to keep the other suites parallel. Saves about a second, costs a root file no `file-classes.yaml` rule covers. A temp-dir corpus discards the real pair.

**Proof:** ten parallel `vitest run` on the unchanged tree — 5/10 failed, every one ENOENT on `DEC-J005`. Ten consecutive `npm run verify` after — 0/10. Single-file proof command still composes with the flag (39 tests, one file).

**Code review:** 1 finding, real, fixed. The context line said "five others read the corpus off disk"; the reviewer traced all ten suites and found **two** (`check-dictionary.test.mjs` via a no-arg `check()`, `drift.test.mjs` via `walk()` recursing `docs/` before the exclusion filter). My count was a string grep across test files, not a read of behaviour — the inventory-in-a-context-doc failure `CLAUDE.md` names. Replaced the number with the mechanism.

**Also answered:** the flake cannot reach muster, soundings or sheepdog. Read from each `origin/main`: none carries a `scripts/*.test.mjs`; they run the gates, not their tests.

**PR:** [PR #34](https://github.com/mobiustripper42/jig/pull/34)
**Points:** 2
**Branch:** task/32-serialize-test-files
**Opened at:** 2026-09-13T04:21:42Z

**Next Steps:**

- **Check the tape first thing.** This is the session that tests it: the hook went into `~/.claude/settings.json` at 2026-09-12T20:05Z, before this session launched, and Step 4.8 wrote `~/.claude/tape/.names/b394aba4-d805-508a-96ff-31d67cdda7b4`. After this window exits, `~/.claude/tape/` should hold `2026-09-13-0048-main.jsonl`. A **uuid-named** file means the hook ran and the name file was not found. **No file at all** means the hook did not fire. **A name file still sitting in `.names/`** means the same. Three distinguishable outcomes — read which one before theorising.
- **The model discussion, still unheld.** Unchanged from Session 2's close and it has now outlived one session. The four things that carry: the Model Selection table pins `claude-fable-5` while the `fable` alias resolves to Fable 5.1; the "Opus within half a percent" line was measured against Fable 5 at `max`; **nothing in jig sets `effort:` in frontmatter**, which is the cheapest untouched lever here; and `settings-policy.mjs:216` watches `effortLevel` while `/effort` may write per-model levels under `modelSettings` — still unverified against the actual file. It wants a chat session with the two Anthropic documents, not a jig session.
- **Three repos owe a sync. The numbers below are Session 2's, not re-measured here** — soundings 7, sheepdog 7, muster 4. Re-run `node scripts/drift.mjs ../<repo>` before acting on any of them; PR #34 changed nothing that ships, so the counts are probably unmoved, but "probably" is not a measurement. Muster's `.claude/output-styles/one-piece.md` is **ahead of jig on purpose** — do not sync that one down.
- **Open issues, none from this session:** #31 (a SPEC section naming a third party must cite its docs), #25 (`check-dictionary` gates `CLAUDE.md`, which a project cannot edit, and skips the context file it owns — rollout cost measured 2026-09-09: sheepdog 3, muster 10, soundings 5, all the acronym rule, no false positives), #24 (nothing checks a code comment).
- **jig issue #26 step 4 remains deliberately unanswered** — may an archived record be edited? It will be the first question asked once anyone archives something.
- **The six dead muster records** from Session 2's sweep are still there, and three of the ten read so far turned out **alive**. Read before archiving; the sweep's criterion (uncitable from code) selects for records that decide *not* to build something.

**Context:**

- **A grep across test files is not a read of what they do.** The context line this session added said five suites read the decision corpus; `@code-review` traced all ten and found two. The other five matched on strings in fixture setup or comments. This is the inventory-in-a-context-doc failure `CLAUDE.md` names, committed in the same edit that was citing the rule — the number felt like a measurement because a command produced it.
- **`--no-file-parallelism` is on `npm run test`, not in a config file.** It composes with a single-file run, so `npm run test -- scripts/<name>.test.mjs` — the Proof command — still works. There is no `vitest.config.*` in jig and adding one would raise a `file-classes.yaml` question for a root path no rule covers.
- **The flake was worse than filed.** Issue #32 measured one run in three; ten runs on a clean tree this session gave **five failures**. Same failure every time, ENOENT on `DEC-J005`. If a flake's rate matters to a decision, measure it rather than quoting the issue.
- **jig's test suites do not ship.** `scripts/*.test.mjs` is `jig-only`, and this was confirmed against `origin/main` for all three siblings: none carries one. Projects run the gates, not their tests — so a defect in a test here cannot reach them, and a defect in a gate reaches all of them.
