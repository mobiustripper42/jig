---
session: 3
slug: main
branch: main
started: 2026-09-13T00:48:11Z
ended:
points:
pr_numbers: [34]
status: open
transcript: /home/eric/.claude/projects/-home-eric-jig/b394aba4-d805-508a-96ff-31d67cdda7b4.jsonl
---

# Session 3 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Issue #32 — run test files one at a time, so the archive tests stop racing the corpus readers

**Completed:**
- `package.json` — `"test": "vitest run --no-file-parallelism"`. One line. Serial adds about 1.4s (2.26s → 3.64s).
- `.claude/CLAUDE-context.md` § Workflow Notes — why, citing issue #32, and why `describe.sequential` would not have done it: it orders tests within a file, and the collision is across files.
- Both archive tests untouched. The pair has to be real (`DEC-J005` cites `DEC-J004`), so the tests stay and the parallelism goes.

**Not built:** a `vitest.config.mjs` with projects and `groupOrder` to keep the other suites parallel. Saves about a second, costs a root file no `file-classes.yaml` rule covers. A temp-dir corpus discards the real pair.

**Proof:** ten parallel `vitest run` on the unchanged tree — 5/10 failed, every one ENOENT on `DEC-J005`. Ten consecutive `npm run verify` after — 0/10. Single-file proof command still composes with the flag (39 tests, one file).

**Code review:** 1 finding, real, fixed. The context line said "five others read the corpus off disk"; the reviewer traced all ten suites and found **two** (`check-dictionary.test.mjs` via a no-arg `check()`, `drift.test.mjs` via `walk()` recursing `docs/` before the exclusion filter). My count was a string grep across test files, not a read of behaviour — the inventory-in-a-context-doc failure `CLAUDE.md` names. Replaced the number with the mechanism.

**Also answered:** the flake cannot reach muster, soundings or sheepdog. Read from each `origin/main`: none carries a `scripts/*.test.mjs`; they run the gates, not their tests.

**PR:** [PR #34](https://github.com/mobiustripper42/jig/pull/34)
**Points:** 2
**Branch:** task/32-serialize-test-files
**Opened at:** 2026-09-13T04:21:42Z

**Next Steps:**

**Context:**
