{
  "name": "jig",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Workflow fixture — templates, gates and skills, carried forward from seeds",
  "scripts": {
    "gen:decisions": "node scripts/gen-decisions-index.mjs",
    "gen:dictionary": "node scripts/gen-dictionary.mjs",
    "check:decisions": "node scripts/check-decisions.mjs",
    "check:dictionary": "node scripts/check-dictionary.mjs",
    "check:context": "node scripts/check-context.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "test": "vitest run",
    "verify": "npm run check:decisions && npm run check:dictionary && npm run check:denied && npm run check:context && npm run check:docs && npm run test",
    "check:denied": "node scripts/check-denied.mjs"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1",
    "vitest": "^4.0.0"
  }
}
